# 交付前验证记录

核查日期：2026-09-24。这里记录实际做过的检查，**不属于论文的 GPU 实验结果**。

| 项目 | 实际验证与结果 |
|---|---|
| 运行环境 | Python 3.12.14、PyTorch 2.6.0+cpu；其余版本见 `environment_cpu.json` |
| 自动测试 | **38 passed，3 skipped**；跳过的三项明确要求真实 CUDA GPU |
| 数值前向与反向 | 四种梯度模式前向相同；因果性、解析径向梯度、标准前向 JVP 与中心差分通过 |
| 恢复 | CPU 上模型、Adam 状态、采样器及随机状态恢复后逐位一致 |
| WikiText 来源 | 实际下载并逐一核验四个官方 Parquet，约315MB；不是只检查网址能打开 |
| WikiText 文档处理 | 修复数学公式被误当文章标题；重建文档单元28472/60/60。训练与测试有一篇精确重复，已从训练排除，保留官方 held-out |
| PG-19 来源 | 实际下载100本官方测试书，共41,289,101 bytes；全部核验固定对象版本、大小、MD5，并记录 SHA-256 |
| Tokenizer | GPT-2 词表50257；原始分词文件校验；移位、EOS、前缀预算与二进制文件校验通过 |
| 真文本端到端 | 2个种子 × S/QD，共4条小模型 CPU 轨迹，每条4步；完整训练、检查点、分叉、汇总链路通过 |
| Adam 分叉重放 | 上述两个种子的步骤0/2/4，共6个检查点；参数增量与注意力 logits 重放最大差均为0 |
| 最终评估链路 | WikiText validation 与 PG-19 test 各128个目标 token 的工程检查；两种独立数据的配对汇总均完成，无错误 |
| 主架构检查 | 真实17,653,248参数模型，CPU 上1个微批次、16个预测 token 的前向/反向/Adam 更新通过；不冒充完整预算训练 |
| 理论有限步检查 | 独立 query 输出阶数1.9993、KL阶数4.0016；共享 W 输出阶数1.0027、KL阶数2.0025；仅适用于脚本中的受控假设 |
| Notebook | 21个单元、12个代码单元通过语法检查；ZIP 首次加载与恢复经本地模拟执行；不是声称真实 Colab 已执行 |
| 确认阶段调度 | 冻结和计划检查通过：12条训练轨迹、24个检查点诊断；没有在此执行这些正式任务 |

小规模真实数据准备只编码4096个训练 token、1024个 WikiText validation/test token 和1024个 PG-19 test token；源文件和全部重建文档均经过审计。**这些小前缀是软件验证夹具，不是正式2000万训练 token数据，也不是正式确认结果。** 正式数据应按 README/Notebook 重新准备，产生属于自己运行的数据清单。

官方论文的 WikiText-103 训练文章数为28475；当前锁定快照按本包明确的孤立一级标题规则重建得到28472个单元。差异没有强制修成一致。规则保留所有原文本行；EOS 插入与去重依据本包重建单元，不能声称恢复了原作者的全部文章 ID。详见 `docs/DATA.md`。精确文档去重不能排除近似或片段重合。

仍待用户 GPU 执行：CUDA FP16/BF16 的3项专用测试、实际显存/速度测量、完整 pilot、确认实验及预定稳健性。不同 CUDA/PyTorch/硬件组合不保证逐比特一致；Notebook 先验证当前环境，再运行实验。

`end_to_end_cpu_audit.json`、`cpu_engineering_execution.log`、两份来源审计和 smoke 数据 manifest 是本次验证证据。工程自检中的 NLL/KL 数字不能用来主张算法优势、机制普适性或期刊接收。
